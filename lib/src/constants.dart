/// A type alias for the possible return types of a JSON parsing operation.
/// It can be a Map, List, or a primitive type.
typedef JSONReturnType = dynamic;

/// A list of characters that are considered string delimiters in JSON.
const List<String> stringDelimiters = ['"', "'", "“", "”"];
